@model IEnumerable<eCommerce.Models.Table.Cliente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdPessoaNavigation.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsPreferencial)
            </th>
            <th>
                Tipo de Pessoa
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IsPreferencial)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdPessoaNavigation.Nome)
                </td>
                <td>
                    @if (item.IdPessoaNavigation.PessoaFisica == null)
                    {
                        <span>Física</span>
                    }
                    else
                    {
                        <span>Jurídica</span>
                    }
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdPessoa">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.IdPessoa">Exibir</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdPessoa">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>
